language: php

php: 
  - 5.3
  - 5.4

branches:
  only:
    - master
    - develop

before_script:
  - php -S localhost:8000 &
  - pyrus install http://pear.behat.org//get/mink-1.3.2.tgz
  - phpenv rehash

script: "php codecept run"