language: php

php: 
  - 5.3
  - 5.4

branches:
  only:
    - master
    - develop

before_script:
  - php -S localhost:8000 &
  - wget http://getcomposer.org/composer.phar
  - pirus channel-discover pear.symfony.com
  - pirus channel-discover pear.behat.org
  - pirus install behat/mink

script: "php codecept run"