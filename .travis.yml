language: php

php: 
  - 5.3
  - 5.4

branches:
  only:
    - master
    - develop

before_script:
  - php -S localhost:8000 &
  - wget http://getcomposer.org/composer.phar
  - pyrus channel-discover pear.symfony.com
  - pyrus channel-discover pear.behat.org
  - pyrus install behat/mink

script: "php codecept run"