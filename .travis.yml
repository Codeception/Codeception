language: php

php: 
  - 5.3
  - 5.4

branches:
  only:
    - master
    - develop

before_script:
  - php -S localhost:8000 &
  - git clone  git://github.com/Behat/Mink.git vendor/Mink && cd vendor/Mink
  - wget -nc http://getcomposer.org/composer.phar
  - php composer.phar install
  - cd ../..

script: "php codecept run"