language: php

php: 
  - 5.3
  - 5.4

branches:
  only:
    - master
    - develop

before_script:
  - php -S localhost:8000 &
  - git clone  git://github.com/Behat/Mink.git mink && cd mink
  - wget -nc http://getcomposer.org/composer.phar
  - php composer.phar install

script: "php codecept run"