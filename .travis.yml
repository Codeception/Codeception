language: php

php:
  - 5.4

install:
  - composer global require "fxp/composer-asset-plugin:1.0.0"
  - composer self-update
  - composer install --prefer-dist

script:
  - php codecept run tests/unit/Codeception/Module/DbTest.php -vv
